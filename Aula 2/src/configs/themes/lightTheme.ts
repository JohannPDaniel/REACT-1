import type { DefaultTheme } from "styled-components";

export const lightTheme: DefaultTheme = {
    backgroundColor: "#fff",
    textColor: "#000",
    primaryColor: "#0d6efd",
    dividerColor: "gray"
}